
import 'package:flutter/cupertino.dart';

class DigitalFlipClock extends StatelessWidget {
  const DigitalFlipClock({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
        child: Text("Clock")
    );
  }
}
