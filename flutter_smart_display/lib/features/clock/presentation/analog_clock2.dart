
import 'package:flutter/cupertino.dart';

class AnalogClock2 extends StatelessWidget {
  const AnalogClock2({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
        child: Text("Clock")
    );
  }
}
