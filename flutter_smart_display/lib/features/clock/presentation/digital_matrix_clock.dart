
import 'package:flutter/cupertino.dart';

class DigitalMatrixClock extends StatelessWidget {
  const DigitalMatrixClock({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
        child: Text("Clock")
    );
  }
}
