
import 'package:flutter/cupertino.dart';

class AnalogJetAlarmClock extends StatelessWidget {
  const AnalogJetAlarmClock({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
        child: Text("Clock")
    );
  }
}
